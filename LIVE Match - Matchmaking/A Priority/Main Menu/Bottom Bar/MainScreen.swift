// MARK: - MainScreen.swift
// Central enum for screens or tabs.

import SwiftUI

@available(iOS 15.6, macOS 11.5, visionOS 2.0, *)
public enum MainScreen {
    case menu
    case feed
    case search
    case messages
    case profile
}
